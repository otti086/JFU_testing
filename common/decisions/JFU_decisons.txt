


JFU_decisions = {	
	GER_fall_weiss = { 
		icon = ger_reichskommissariats
		fire_only_once = yes		
		allowed = { tag = GER }	
		available = { has_completed_focus = GER_danzig_or_war	}		
		cost = 0
		complete_effect = { 
            declare_war_on = {
                	target = POL
                    type = annex_everything
            }
			if = { limit = { country_exists = HUN }	HUN = { add_to_war = { targeted_alliance = GER enemy = ENG } } }
			if = { limit = { country_exists = ROM }	ROM = { add_to_war = { targeted_alliance = GER enemy = ENG } } }
			if = { limit = { country_exists = BUL }	BUL = { add_to_war = { targeted_alliance = GER enemy = ENG } } }			
			hidden_effect = { 
				ENG = { add_to_faction = POL } 
				declare_war_on_all_allies = yes
				country_event = { id = germany.110 days = 3 }   
			}
		}
	}
	GER_fall_gelb = {
		icon = ger_reichskommissariats
		fire_only_once = yes	
		allowed = { 	tag = GER 	}
		available = { has_war_with = ENG	}
		cost = 0
		complete_effect = {
			hidden_effect = {  ENG = { add_to_faction = HOL add_to_faction = BEL }  }
			declare_war_on = {
				target = HOL
				type = annex_everything
			}
				declare_war_on = {
				target = BEL
				type = annex_everything
			}	
					hidden_effect = { 
					ROOT_call_against_beligum = yes  ROOT_call_against_holland = yes
			}
		}
	}
	GER_operation_weserubung = { 
		icon = ger_reichskommissariats
		fire_only_once = yes	
		allowed = { 	tag = GER 	}
		available = { 	has_war_with = ENG	}	
		cost = 0		
		complete_effect = {				
			add_tech_bonus = {
				name = tp_bonus
				bonus = 1.0
				ahead_reduction = 2
				uses = 1
				category = tp_tech
			}
			hidden_effect = {  	ENG = { add_to_faction = DEN }  }
			declare_war_on = {
				target = DEN
				type = annex_everything
			}	
					hidden_effect = { 
				ROOT_call_against_denmark = yes 
			}		
	
		}	
	}
	
	GER_operation_weserubung2 = { 
		icon = ger_reichskommissariats
		fire_only_once = yes	
		allowed = { 	tag = GER 	}
		available = {  NOT = { DEN = { controls_state = 99 }}	}	
		visible = { NOT = { has_war_with = NOR }}
		cost = 0		
		complete_effect = {
			declare_war_on = {
				target = NOR
				type = annex_everything
			}	
			hidden_effect = {  
				ROOT_call_against_norway = yes 
			}
		}	
	}
	GER_unt_barb = { 
		icon = ger_reichskommissariats
		fire_only_once = yes	
		allowed = { 	tag = GER 	}
		available = { 	date > 1941.6.21	}	
		cost = 0		
		complete_effect = {
			declare_war_on = {
			target = SOV 
			type = annex_everything
			}
			if = { limit = { country_exists = HUN }	HUN = { add_to_war = { targeted_alliance = GER enemy = SOV } } }
			if = { limit = { country_exists = ROM }	ROM = { add_to_war = { targeted_alliance = GER enemy = SOV } } }
			if = { limit = { country_exists = BUL }	BUL = { add_to_war = { targeted_alliance = GER enemy = SOV } } }
			GER = { add_timed_idea = { idea = axis_barbarossa days = 180 } }
			hidden_effect = {
			HUN = { add_timed_idea = { idea = axis_barbarossa days = 180 } }
			ROM = { add_timed_idea = { idea = axis_barbarossa days = 180 } }	
			BUL = { add_timed_idea = { idea = axis_barbarossa days = 180 } }	
			SPR = { add_timed_idea = { idea = axis_barbarossa days = 180 } }
			}
		}
	}
	GER_operation_desert_flower = {
		icon = generic_operation
		fire_only_once = yes
		allowed = { tag = GER }
		available = {	GER = { controls_state = 454 }	} 
		cost = 0
		complete_effect = {
			hidden_effect = {  	ENG = { add_to_faction = IRQ }  }
			declare_war_on = {
			target = IRQ 
			type = annex_everything
			}
			if = { limit = { country_exists = ITA }	ITA = { add_to_war = { targeted_alliance = GER enemy = IRQ } } }
			if = { limit = { country_exists = HUN }	HUN = { add_to_war = { targeted_alliance = GER enemy = IRQ } } }
			if = { limit = { country_exists = ROM }	ROM = { add_to_war = { targeted_alliance = GER enemy = IRQ } } }
			if = { limit = { country_exists = BUL }	BUL = { add_to_war = { targeted_alliance = GER enemy = IRQ } } }
		}		
	}





	JAP_attack_china = {
		icon = generic_operation
		fire_only_once = yes
		allowed = { tag = JAP }
		available = { has_completed_focus = JAP_intervene_in_china  }
		cost = 0
		complete_effect = {
			declare_war_on = {
			target = CHI 
			type = annex_everything
			}
			if = { limit = { country_exists = MAN }	MAN = { add_to_war = { targeted_alliance = JAP enemy = CHI } } }
			add_ideas = JAP_intervene_in_china_5
			unlock_decision_tooltip = JAP_china_step_up_war_effort
			hidden_effect = {
				country_event = { days = 5 id = news.1 }
			}
			USA = { set_country_flag = { flag = USA_china_window days = 60 value = 1 } }
			if = {
				limit = {
					has_country_leader = { id = 700 ruling_only = yes }
				}
				remove_country_leader_trait = imperial_sanction
			}
		}

	}




	JAP_attack_allies = {
		icon = generic_operation
		fire_only_once = yes
		allowed = { tag = JAP }
		available = {	date > 1941.1.1	} 
		cost = 0
		complete_effect = {
			declare_war_on_all_allies = yes
			if = { limit = { country_exists = MAN }	MAN = { add_to_war = { targeted_alliance = JAP enemy = ENG } } }
			if = { limit = { country_exists = SIA }	SIA = { add_to_war = { targeted_alliance = JAP enemy = ENG } } }
			add_timed_idea = { idea = JAP_tora_tora_tora_idea days = 180 }
			MAN = { add_timed_idea = { idea = JAP_tora_tora_tora_idea days = 180 } }
			SIA = { add_timed_idea = { idea = JAP_tora_tora_tora_idea days = 180 } } 			
	
		}
	}



	JAP_annex_Siam = {
		icon = generic_operation
		fire_only_once = yes
		allowed = { tag = JAP}
		cost = 0
		complete_effect = {
			annex_country = { target = SIA transfer_troops = yes }
			hidden_effect = { 
				every_state = {
					limit = {
						AND = {
							is_core_of = SIA
							OR = {
								is_controlled_by = JAP
							}
						}
					}
				
				add_compliance = 90 
				}
			}
		}
	}


	JAP_annex_Manchu = {
		icon = generic_operation
		fire_only_once = yes
		allowed = { tag = JAP }
		cost = 0
		available = {	date < 1937.1.1	} 
		complete_effect = {
			annex_country = { target = MAN transfer_troops = yes }
			hidden_effect = { 
				every_state = {
					limit = {
						AND = {
							is_core_of = MAN
							OR = {
								is_controlled_by = JAP
							}
						}
					}
				
				add_compliance = 90 
				}
			}
		}
	}


	AST_annex_NEW_Zealand = {
		icon = generic_operation
		fire_only_once = yes
		allowed = { tag = AST }
		cost = 0
		complete_effect = {
			annex_country = { target = NZL transfer_troops = yes }
			hidden_effect = { 
				every_state = {
					limit = {
						AND = {
							is_core_of = NZL
							OR = {
								is_controlled_by = AST
							}
						}
					}
				
				add_compliance = 100
				}
			}
		}
	}



	CAN_annex_South_Africa = {
		icon = generic_operation
		fire_only_once = yes
		allowed = { tag = CAN }
		cost = 0
		complete_effect = {
			annex_country = { target = SAF transfer_troops = yes }
			hidden_effect = { 
				every_state = {
					limit = {
						AND = {
							is_core_of = SAF
							OR = {
								is_controlled_by = CAN
							}
						}
					}
				
				add_compliance = 100
				}
			}
		}
	}





	ITA_establish_IEA = {
		allowed = { original_tag = ITA }
		available = { 
			has_completed_focus = ITA_victoryinETH 
			owns_state = 271 owns_state = 550 owns_state = 559 
		}
		
		fire_only_once = yes
		
		complete_effect = { 
			ETH = {
				transfer_state = 271
				transfer_state = 550
				transfer_state = 559
				
				271 = { add_core_of = ETH }
				550 = { add_core_of = ETH }
				559 = { add_core_of = ETH }
				
				inherit_technology = ITA	
			}
			
			puppet = ETH
		
		}
	}	




	ITA_attack_Yugoslavia = {
		icon = generic_operation
		fire_only_once = yes
		allowed = { tag = ITA }
		available = {	has_completed_focus = ITA_slovenia_dalmatia_claims } 
		cost = 0
		complete_effect = {
            declare_war_on = {
                        target = YUG
                        type = annex_everything			
			}
			add_named_threat = { threat = 6 name = ITA_Balkan_expantion1 }
			hidden_effect = { 
				if = { limit = { country_exists = HUN }	HUN = { add_to_war = { targeted_alliance = GER enemy = YUG } } }
				if = { limit = { country_exists = ROM }	ROM = { add_to_war = { targeted_alliance = GER enemy = YUG } } }
				if = { limit = { country_exists = BUL }	BUL = { add_to_war = { targeted_alliance = GER enemy = YUG } } }
			}	
		}
	}





	ITA_attack_Greece = {
		icon = generic_operation
		fire_only_once = yes
		allowed = { tag = ITA }
		available = {	has_completed_focus = ITA_greece_war_goal } 
		cost = 0
		complete_effect = {
            declare_war_on = {
                        target = GRE
                        type = annex_everything
            }
			add_named_threat = { threat = 2 name = ITA_Balkan_expantion2 }				
			hidden_effect = { 
				if = { limit = { country_exists = HUN }	HUN = { add_to_war = { targeted_alliance = GER enemy = GRE } } }
				if = { limit = { country_exists = ROM }	ROM = { add_to_war = { targeted_alliance = GER enemy = GRE } } }
				if = { limit = { country_exists = BUL }	BUL = { add_to_war = { targeted_alliance = GER enemy = GRE } } }
			}	
		}
	}




	ITA_joins_WW2 = {
		icon = generic_operation
		fire_only_once = yes
		allowed = { tag = ITA }
		available = { GER = { controls_state = 16 } }
		cost = 0
		complete_effect = {
			ITA = { add_to_war = { targeted_alliance = GER enemy = ENG } }
		}
	}	

	send_air_to_ger = {
	
		icon = GFX_decision_generic_air
			
			allowed = {
		}
		
		available = {} 
		fire_only_once = no
		visible = {
			original_tag = ITA
			NOT = {has_country_flag = ITA_air_LL}
		}
		
		complete_effect = {
			activate_mission = send_air_to_ger1 set_country_flag = ITA_air_LL
		}			
	}
	
	send_air_to_ger1 = {
	
		icon = GFX_decision_generic_air
			
			allowed = {
		}
		
		available = {NOT = {has_country_flag = ITA_air_LL}} 
		fire_only_once = no
		visible = {
			original_tag = ITA
			always = yes
		}
		days_mission_timeout = 7
		is_good = yes
		activation = { always = no	}
		
		timeout_effect = {
			if = { 
				limit =  { has_country_flag = ITA_air_LL 
				}
			air_to_germany = yes
			activate_mission = send_air_to_ger1
			}
		}	
	}
	
	cancel_air_to_ger = {
	
		icon = GFX_decision_generic_air
			
			allowed = {
		}
		
		available = {always = yes} 
		fire_only_once = no
		visible = {
			original_tag = ITA
			has_country_flag = ITA_air_LL
		}
	
		complete_effect = {
			if = { 
			limit =  { has_country_flag = ITA_air_LL 
				}
				clr_country_flag = ITA_air_LL
			}
		}	
	}

	UK_declare_on_Vichy_France = {

		icon = generic_operation

		allowed= { tag = ENG }

		available = {date > 1942.1.1}
		fire_only_once = yes
		cost = 0
		complete_effect = {
			GER = { add_to_faction = EFR }
			EFR = { add_to_war = { targeted_alliance = GER enemy = ENG } }
		}
	}

	"Support France" = {
		icon = eng_install_government
		allowed = { original_tag = ENG }
		days_remove = 120

		available = {NOT = { FRA = { owns_state = 16 }}  check_variable = { FRA.FRA_legitimacy < 100 }
						FRA = { has_country_flag = france_is_kill}}
		visible = {	tag = ENG }
		cost = 50
		complete_effect = {
		custom_effect_tooltip = "French Legitimacy will increase by 20"
			add_to_variable =  { var = FRA.FRA_legitimacy value = 20 }
		}
	}
}